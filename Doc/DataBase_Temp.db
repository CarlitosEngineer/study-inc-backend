
CREATE TABLE genders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE
);

CREATE TABLE languages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    iso639_1 TEXT NOT NULL UNIQUE CHECK (LENGTH(iso639_1) = 2),
    iso639_2 TEXT NOT NULL UNIQUE CHECK (LENGTH(iso639_2) = 3)
);

CREATE TABLE countries (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    iso3166_2 TEXT NOT NULL UNIQUE CHECK (LENGTH(iso3166_2) = 2),
    iso3166_3 TEXT NOT NULL UNIQUE CHECK (LENGTH(iso3166_3) = 3),
    num_code TEXT NOT NULL UNIQUE CHECK (LENGTH(num_code) = 3),
    phone_code TEXT NOT NULL UNIQUE CHECK (LENGTH(phone_code) BETWEEN 1 AND 4)
);


¿Quieres mejorar la redacción, hacerla más atractiva o agregar más detalles? Aquí tienes una versión mejorada:

---

**Descripción del Proyecto**  

Este es un **proyecto backend** diseñado para **crear, almacenar, documentar y editar frases**. Permite gestionar frases tanto **famosas como inéditas**, incluyendo **citas de libros, autores, científicos, películas y más**. Su objetivo es ofrecer una plataforma flexible para organizar y recuperar frases de manera eficiente.

---

Si necesitas agregar más detalles técnicos (tecnologías usadas, API endpoints, etc.), dime y lo ajustamos. 🚀